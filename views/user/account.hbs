{{#if user}}
    <div class="well">
        <h3>{{user.name}} Dashboard</h3>
        <p class="text text-muted">{{user.email}}</p>
        <form method="post" action="/friend/add/{{id}}">
            <a class="btn btn-primary" type="submit" href="">Add Friend</a>
        </form>
    </div>
{{/if}}
