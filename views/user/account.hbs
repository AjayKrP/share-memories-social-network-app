{{#if user}}
    <div class="well">
        <h3>{{user.name}} Dashboard</h3>
        <p class="text text-muted">{{user.email}}</p>
        <a class="btn btn-primary" onclick="addFriend({{user.id}})">Add Friend</a>
    </div>
{{/if}}
